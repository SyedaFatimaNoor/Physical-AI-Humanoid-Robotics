# Backend API URL
# For local development, use http://localhost:8003
# For production (Vercel), use your deployed backend URL or leave empty to use relative paths
REACT_APP_API_URL=

# Leave empty to use relative paths (/api/...) when deployed together
# Set to http://localhost:8003 for local development with separate backend
